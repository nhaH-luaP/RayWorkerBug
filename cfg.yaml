defaults:
  - _self_

num_opt_samples: 50
num_cpus: 16
num_cpus_per_trial: 4
num_gpus: 1
num_gpus_per_trial: 0.25

output_dir: ./output/
hydra:
  run:
    dir: ${output_dir}
  job_logging:
    handlers:
      file:
        mode: w

